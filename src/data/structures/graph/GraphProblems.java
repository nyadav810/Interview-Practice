package data.structures.graph;

public class GraphProblems {
   public static void main(String[] args) {
      Graph g = new Graph();
      g.addEdge(1, 10);
      g.addEdge(2, 10);
      g.addEdge(3, 10);
      g.addEdge(4, 5);
      g.print();
   }
}